/*import { test } from '@playwright/test';

test('BrowserStack Inception via Automate', async ({ page }) => {
  // Go to BrowserStack
  await page.goto('https://www.browserstack.com');
  
  // Login
  await page.click('#signIn');
  await page.fill('#user_email_login', 'demo-email');
  await page.fill('#user_password', 'demo-password'); 
  await page.click('#user_submit');
  
  // Go to Live
  await page.click('text=Live');
  
  // Now how do I grab elements on Live?

});*/